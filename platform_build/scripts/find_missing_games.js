const fs=require('fs'), path=require('path'); const root=path.resolve(__dirname,'..'); const out=[]; for(let i=0;i<=36;i++){ const name=`jeux${i}.html`; const rootP=path.join(root,name); if(fs.existsSync(rootP)) continue; const sources=[path.join(root,'platform_canonical',name), path.join(root,'docs',name)]; let found=null; for(const s of sources){ if(fs.existsSync(s)){ found=s; break; } } out.push({id:i,name,found}); } console.log(JSON.stringify(out,null,2));